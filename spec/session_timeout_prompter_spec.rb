require "spec_helper"

RSpec.describe SessionTimeoutPrompter do
  it "has a version number" do
    expect(SessionTimeoutPrompter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
